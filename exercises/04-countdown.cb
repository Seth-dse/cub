// Exercise 4 — Countdown
//
// Tutorial: section 5 (repeating).
//
// Three loops:
//   1. a `while` that counts 5 down to 1, then prints "liftoff"
//   2. a `for` over 1..=5 printing "1 2 3 4 5 " on one line
//   3. a `for` over 1..=10 that skips odd numbers with `continue`
//      and stops after 8 with `break`, giving "2 4 6 8 "
//
// `write` prints without a newline; `print("")` ends the line.

void main() {
    var n = 5;
    // TODO: the while loop, then "liftoff"

    // TODO: 1 2 3 4 5

    // TODO: 2 4 6 8
}
