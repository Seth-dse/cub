enum Light { Red, Green, Amber }

fn next(current: Light): Light {
    if current == Light.Red {
        return Light.Green;
    } else if current == Light.Green {
        return Light.Amber;
    }
    return Light.Red;
}

fn advice(current: Light): string {
    if current == Light.Red {
        return "stop";
    } else if current == Light.Green {
        return "go";
    }
    return "slow down";
}

fn main() {
    var light = Light.Red;
    for i in 0..6 {
        print("{light}: {advice(light)}");
        light = next(light);
    }
}
