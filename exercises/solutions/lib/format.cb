import math;

/// Rounds a number to two decimal places.
fn round2(v: float): float {
    return math.round(v * 100.0) / 100.0;
}

/// Lays a label and a value out in a fixed-width column.
fn column(label: string, width: int): string {
    return pad_end(label, width, ".");
}
