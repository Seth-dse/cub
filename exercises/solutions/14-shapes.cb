import math;

class Shape {
    name: string;

    void init(name: string) {
        self.name = name;
    }

    float area() {
        return 0.0;
    }

    string to_string() {
        return "{pad_end(self.name, 10, ".")} {math.round(self.area() * 100.0) / 100.0}";
    }
}

class Rect extends Shape {
    width: float;
    height: float;

    void init(width: float, height: float) {
        super.init("rectangle");
        self.width = width;
        self.height = height;
    }

    float area() {
        return self.width * self.height;
    }
}

class Square extends Rect {
    void init(side: float) {
        super.init(side, side);
        self.name = "square";
    }
}

class Circle extends Shape {
    radius: float;

    void init(radius: float) {
        super.init("circle");
        self.radius = radius;
    }

    float area() {
        return math.PI * self.radius * self.radius;
    }
}

void main() {
    let shapes: [Shape] = [Rect(3.0, 4.0), Square(5.0), Circle(1.0)];

    var total = 0.0;
    for s in shapes {
        print(s);
        total += s.area();
    }
    print("total {math.round(total * 100.0) / 100.0}");

    var biggest = shapes[0];
    for s in shapes {
        if s.area() > biggest.area() {
            biggest = s;
        }
    }
    print("largest is the {biggest.name}");
}
