// Exercise 3 — Grading
//
// Tutorial: section 4 (deciding).
//
// `grade` gives back "A" for 90 and above, "B" for 80, "C" for 70, and
// "F" for anything less.
//
// The last line wants an `if` used as a value:
//   let word = if n % 2 == 0 { "even" } else { "odd" }

string grade(score: int) {
    return "?"; // TODO
}

void main() {
    for score in [95, 83, 71, 40] {
        print("{score}: {grade(score)}");
    }

    let n = 7;
    // TODO: print "7 is odd" using an if that produces a value
}
