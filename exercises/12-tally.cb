// Exercise 12 — Counting words
//
// Tutorial: section 10 (maps).
//
// `tally` lower-cases the text, splits it on spaces, and counts each word
// in a map.  `get(counts, word, 0)` gives the count so far, or 0 when the
// word is new — that is the trick that makes this short.
//
// Skip empty pieces, or two spaces in a row will add a "" entry.
//
// Map keys come out in no useful order, so sort them before printing.

[string: int] tally(text: string) {
    var counts: [string: int] = [:]
    // TODO
    return counts
}

void main() {
    let counts = tally("the quick brown fox the lazy dog the end")

    // TODO: print how many distinct words, then counts["the"]
    // TODO: print has(...) for "fox" and "cat", then get(..., "cat", 0)

    // TODO: sort the keys, then print each as
    //       "{pad_end(word, 8, ".")} {counts[word]}"
}
